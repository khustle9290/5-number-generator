#!/bin/bash

# Navigate to your project folder
cd /c/Users/vin/Downloads/5-number-generator || exit

# Pull latest changes from GitHub (rebase to avoid merge commits)
git pull origin main --rebase

# Stage all files (ensures app.py and others are included)
git add .

# Commit the changes (skip if nothing to commit)
git commit -m "Update project files" || echo "No changes to commit"

# Push to GitHub
git push origin main

# Open the repo in your default browser (Windows Git Bash)
repo_url="https://github.com/khustle9290/5-number-generator"
cmd.exe /c start "$repo_url"
